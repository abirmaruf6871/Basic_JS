/* Type Conversion */

// Conversion String to numbers
let pi=Math.PI
// console.log(typeof(pi), typeof(Number(pi)));
// console.log(pi,parseFloat(pi),parseInt(pi));

// Convesion number to string

let y= String(1234+"34")
// console.log(y);
let x=554

// console.log(x,x.toString());

// conversion boolean to string
// console.log(false,String(false),false.toString());

// conversion boolean to number
console.log(Number(false),Number(true));